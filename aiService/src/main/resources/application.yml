spring:
  application:
    name: aiService
  data:
    mongodb:
      uri: mongodb+srv://e%2Dshop:e%2Dshop@microservice-freelence.f8tew3n.mongodb.net/AiFitnessRecommedation?retryWrites=true&w=majority
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: activity-processing-group
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
    properties:
      spring.json.trusted.packages: "*"
      spring.json.value.type.headers: false
      spring.json.value.default.type: package com.AiFitness.aiService.model.Activity



server:
  port: 8083


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

kafka:
  topic:
    name: activity-events

gemini:
  api:
    url: "temp"
    key: "temp"
 